# Dyafa Sales OS - Build Tasklist

Legend: [x] Done (real working code)  [~] Partial / stub  [ ] Not built

- [x] Auth & Roles - Session-based login for 7 staff roles + Corporate Client, `Dso_Controller::require_role()` gate, seeded users with real bcrypt hashes. Files: `application/core/Dso_Controller.php`, `application/controllers/dyafa/Auth.php`, `application/config/dso_roles.php`.
- [x] Lead Management - Full CRUD (add/edit/view/delete/assign) on `dso_leads`, auto-assign new leads to HOD Sales, HOD-only reassignment. File: `application/controllers/dyafa/Leads.php`.
- [~] AI Lead Generation / Scoring - Deterministic heuristic scoring engine (revenue/room-nights/priority/source weighted formula) mapping to Hot/High/Medium/Low/Discard + suggested next action; explicitly documented as a placeholder for a future real ML/AI service, not real machine learning. File: `application/libraries/Dso_lead_scoring.php`. Auto-generation of candidate leads (not just scoring) added below under Missing Features Backlog.
- [x] Contract Management - Full CRUD + funnel view grouped by status with real SQL counts. File: `application/controllers/dyafa/Contracts.php`.
- [x] Corporate Accounts - Full CRUD on `dso_accounts`. File: `application/controllers/dyafa/Accounts.php`.
- [x] Activities - Activity log per account (Call/Meeting/Visit/etc.) with inline add-activity form. Same controller (`Accounts::add_activity`).
- [x] Self-Service Portal - Separate login flow for Corporate Client role, all queries filtered by `account_id`, reservation create/cancel with server-side ownership check, HTML statement (no PDF, not required). File: `application/controllers/dyafa/Portal.php`.
- [x] Reservation Management - CRUD with real validation: allowed-properties check against linked contract, and credit-limit check against outstanding collections + new reservation amount. File: `application/controllers/dyafa/Reservations.php` + `application/models/dyafa/Dso_reservations_model.php`.
- [x] Adhoc Sales - Full CRUD for Wedding/Birthday/MeetingRoom/etc. events with a funnel-style status set (Inquiry, ProposalSent, Negotiation, Confirmed, Completed, Cancelled, Lost). File: `application/controllers/dyafa/Adhoc.php`.
- [x] Sales Targets - CRUD for target rows; actuals computed live via SQL aggregates (no stored actuals columns); `performance()` view with %-achievement per metric and band mapping (Outstanding/Excellent/Good/Average/NeedsAttention). Files: `application/controllers/dyafa/Targets.php`, `application/models/dyafa/Dso_targets_model.php`.
- [x] Collections - CRUD + manual payment recording (paid_amount) with auto status derivation; aging report bucketed 0-30/31-60/61-90/90+ via real SQL DATEDIFF. File: `application/controllers/dyafa/Collections.php`.
- [x] Daily Sales Dashboard - Today's revenue, MTD revenue, reservations/activities/leads today, personal target achievement - all real SQL for the logged-in user. `Dashboard::daily()`.
- [x] HOD Sales Dashboard - Team ranking by MTD revenue/achievement, lead conversion rate per owner, top 5 accounts by revenue, outstanding collections total, contracts expiring in 30 days - restricted via `require_role`. `Dashboard::hod()`.
- [~] AI Sales Assistant - Deterministic rule-based heuristic (documented placeholder for a future real AI/ML service, same pattern as lead scoring), not real ML. Detects accounts inactive 45+ days and contracts expiring within 30 days, generates stored recommendations (suggested action/property/estimated revenue/priority/reason) via a Cron generator with de-dup, same shape as `dso_generate_notifications()`. Files: `application/libraries/Dso_sales_assistant.php`, `application/controllers/Cron.php::dso_generate_ai_recommendations()`, `application/controllers/dyafa/AiAssistant.php`.
- [x] Property Management - Master `dso_properties` (+ `dso_property_rates`) table, Coordinator-only create/edit/delete/upload (map/info file), read-only list for other roles. Reservation/contract forms now pick from this list via dropdown/checkboxes but still submit plain name strings, so the existing allowed-properties string-matching validation is unchanged. Adhoc sales gained an optional `venue_property_id` link. Files: `application/controllers/dyafa/Properties.php`, `application/models/dyafa/Dso_properties_model.php`, `application/models/dyafa/Dso_property_rates_model.php`.
- [~] PMS Integration - Three modes via `dso_pms_mode` (`application/config/dso_integrations.php`): `live` attempts a config-gated HTTP POST to `dso_pms_endpoint`; `mock` (default) calls `Dso_pms_mock` to generate a realistic confirmation number/room/status and persists it on `dso_reservations.pms_reference/pms_room_no/pms_status/pms_synced_at`; `off` is the original log-only stub. Status changes (edit/cancel) also refresh the mock/live PMS state via `sync_pms_status_change()`. Files: `application/libraries/Dso_pms_mock.php`, `application/models/dyafa/Dso_reservations_model.php`, `application/controllers/dyafa/Reservations.php`. Reference column shown in `dyafa/reservations/list`. Migration: `dyafa_sales_os_migration_002_integration_mocks.sql`.
- [~] Finance/ERP Integration - Same three-mode pattern via `dso_finance_mode`: `live` HTTP POST, `mock` (default) calls `Dso_finance_mock` to generate a ledger reference/status persisted on `dso_collections.finance_reference/finance_synced_at`, `off` is the log-only stub. Invoked from `Collections::edit()` on Paid/PartiallyPaid. Files: `application/libraries/Dso_finance_mock.php`, `application/libraries/Dso_finance_integration.php`. Reference column shown in `dyafa/collections/list`.

## Missing Features Backlog (from BRD gap analysis, 2026-07-22)

Legend unchanged. These are not yet built; tracked here so future sessions pick up from a single source of truth.

- [~] AI Lead Generation - real *auto-generation* engine added: `Cron::dso_generate_leads()` + `application/libraries/Dso_lead_generator.php` synthesizes candidate leads from seeded `dso_market_intelligence` signals (industry/city/company-size/avg revenue/avg room nights/signal strength), scored through the existing `Dso_lead_scoring` heuristic and inserted with `source = 'AI Generated'`, de-duped by company name. Explicitly documented as SYNTHETIC PLACEHOLDER DATA, not a real data-acquisition provider (Apollo/ZoomInfo-type API or scraping) - that remains a budget/legal decision for the user. Migration: `dyafa_sales_os_migration_007_ai_lead_gen.sql`.
- [x] AI Sales Assistant - upgraded beyond free-text-only LLM enhancement: `Dso_llm_client::suggest_property_and_priority()` lets the configured LLM propose a `suggested_property_id`/`priority` candidate, but `Dso_sales_assistant::_apply_llm_candidate_fields()` always validates it against the real active property list and the Low/Medium/High enum before applying - mismatches are logged and the heuristic's own value is kept. `estimated_revenue` is still never touched by the LLM (only the heuristic's trailing-average/credit-limit-ratio logic sets it), per the BRD/enhance.md caution against fabricated revenue numbers.
- [x] Company User Management - `dso_users.role` ENUM extended with 5 corporate sub-roles (`CorporateAdmin`/`CorporateHR`/`CorporateFinance`/`CorporateTravelCoordinator`/`CorporateProjectManager`, legacy flat `Corporate Client` kept for backward compatibility), each scoped by the existing `account_id` column. `dso_corporate_capabilities` in `application/config/dso_roles.php` maps each sub-role to capabilities (manage_users/create_reservation/modify_reservation/cancel_reservation/view_statement) matching BRD Section 11, enforced via new `Dso_Controller::require_corporate_capability()`. CorporateAdmin gets a Company User Management screen (`Portal::users()`/`user_add()`/`user_toggle_status()`). Migration: `dyafa_sales_os_migration_006_corporate_subroles.sql`.
- [x] Corporate Self-Service Portal - hotel/availability search (`Portal::search()`) lists properties allowed by the account's contract with the account's corporate rate (falling back to the property's standard rate), feeding into `reservation_new` via a prefilled query string. Invoice download (`Portal::invoice_download()`) renders a single collection/invoice as a PDF via the already-present `Dompdf_lib`, ownership-checked against the logged-in account. book.dyafa.com subdomain routing remains out of scope (this app is served from one vhost, not multi-tenant subdomains).
- [x] Reports Management - all 10 remaining report types built: Reservations, Room Nights, Contract, Contract Renewal, Opportunities, Adhoc Sales, Activities, Corporate Accounts, Property Performance, AI Recommendation (14 reports total now). CSV export added via shared `application/helpers/dso_csv_helper.php` (`dso_export_csv()`) and wired into every report (`?export=csv`), including the original 4. A "Push to Reporting Platform" action (`Reports::push_to_reporting()`, HOD-only) is also wired on 7 of the reports. Files: `application/controllers/dyafa/Reports.php`, `application/views/dyafa/reports/*`, small new aggregate methods on `Dso_reservations_model`, `Dso_adhoc_model`, `Dso_activities_model`, `Dso_properties_model::performance()`. Sidebar updated in `application/config/dso_menu.php`.
- [x] Notification Management - dedicated triggers for VIP Guest Arrival, Guest Complaints, Proposal Pending Approval added to `Cron::dso_generate_notifications()` (`_notify_guest_complaints()`, `_notify_proposal_pending()`, `_notify_vip_arrival()`), alongside the existing new-lead/payment-pending/contract-expiring/target-achieved triggers. VIP arrival required a new `dso_accounts.is_vip` flag (`dyafa_sales_os_migration_003_vip_notifications.sql`), now toggleable on the Account form.
- [x] Property Management - real geo maps added: `dso_properties.lat`/`lng` (migration `dyafa_sales_os_migration_004_property_geo.sql`), auto-geocoded from city via `Dso_maps_mock` (`dso_maps_mode` in `dso_integrations.php`, same live/mock/off convention as PMS/Finance) when left blank on the form, rendered as an embedded Leaflet map on the property form and an OpenStreetMap link on the list. Static map/info file upload (pre-existing) is unchanged.
- [x] Integrations - Payment Gateway and Reporting Platform now have the same config-gated mock-adapter structure as PMS/Finance: `dso_payment_mode`/`dso_reporting_mode` in `dso_integrations.php`, `Dso_payment_mock.php`/`Dso_payment_integration.php` (persists `dso_collections.payment_reference`/`payment_synced_at`, migration `dyafa_sales_os_migration_005_payment_mock.sql`, invoked from `Collections::edit()` alongside Finance sync), and `Dso_reporting_mock.php`/`Dso_reporting_integration.php` (invoked via `Reports::push_to_reporting()`). Maps Services covered above under Property Management.
- [x] Reports - `daily_sales()`, `revenue()` (by month/property/account), `aging()`, `leads()` (by status/category/owner) - all real SQL, HTML tables, now with CSV export. File: `application/controllers/dyafa/Reports.php`.
- [x] Notifications - Real SQL-driven generator (`Cron::dso_generate_notifications`) for new-lead, payment-pending, contract-expiring, target-achieved, guest-complaint, proposal-pending, and VIP-arrival events with de-duplication logic; in-app `dso_notifications` table only. Actual email/SMS/push delivery is explicitly out of scope and documented as such. Files: `application/controllers/Cron.php`, `application/controllers/dyafa/Notifications.php`.
- [x] Auth & Roles - see first line above (session auth + role-based access control across all controllers), now including 5 corporate sub-roles.

## Notes
- Payment gateway is a mock-adapter integration (see above), not a fully automated live gateway - collections are still recorded manually by Finance Team; the mock reference is generated only after a manual payment update.
- Maps service now used for Property Management geocoding (mock by default); `book.dyafa.com` as a literal separate subdomain remains out of scope, the portal is served from the same vhost as the internal app.
- All 5 new migrations (`003`-`007`) are additive/idempotent where MySQL allows it (guarded by `information_schema` checks) except `006`'s ENUM `MODIFY COLUMN`, which is safe to re-run as-is. Run them in order after the base schema.
- **Correction (this session, 2026-07-22)**: the Reports section above and in `apiurl.md`'s old menu table understated the build - all 14 report types have been live since the prior session; the sidebar's Reports group already lists all 14. No further action needed there.

## BRD Sidebar Gap Closure (this session, 2026-07-22)

Closes every remaining gap between the full BRD sidebar spec (originally captured in `apiurl.md`) and the live menu. All items below are real working CRUD/list/aggregate code following the pre-existing controller/model/view pattern - nothing is a placeholder view with no backing query, except where explicitly noted (AI Predictions/Next Best Actions, see below).

- [x] Dashboard - `Dashboard::my_performance()` (own current-month target vs actual + recent activity) and `Dashboard::team_performance()` (HOD/Manager/Management-gated, per-team aggregate via `Dso_teams_model::performance()`).
- [x] Leads - `Leads::index($scope)` now supports `mine`/`unassigned`/`ai` scopes reusing the existing list view; `Leads::sources()` groups leads by source with counts/revenue.
- [x] **AI Lead Generation** - new standalone menu group. `dso_lead_scoring_config` table (migration `008`) lets HOD Sales/Management tune the 4 scoring weights via `LeadScoringConfig` controller; `Dso_lead_scoring` reads from it with a fallback to its original hardcoded weights so behavior is unchanged until a weight is edited. `LeadGeneration` controller triggers the same synthetic-lead engine synchronously (logic extracted into `Dso_lead_generator::generate()`, reused by both the controller and `Cron::dso_generate_leads()`). "Recommendations"/"AI Settings" are cross-links to the existing `AiAssistant`/`AiConfig` controllers, no duplicate code.
- [x] Contracts - `Contracts::index($status)` supports `active`/`pending`/`expiring` (30-day expiry window) filters, territory-scoped for Teams.
- [x] Corporate Accounts - `Accounts::view360($id)` aggregates contract + reservations + collections + activities on one page; `Accounts::performance()` lists per-account revenue/room-nights/reservation-count.
- [x] Reservations - `Reservations::index($filter)` supports `pending`/`checkins_today`/`checkouts_today`; `Reservations::calendar()` is a server-rendered month grid with AJAX drag-drop (`calendar_move()`) that reuses the existing `validate_against_contract()` path, never bypassing it.
- [x] Adhoc Sales - `Adhoc::board()` is a server-rendered kanban (one column per status) with AJAX drag-drop (`board_move()`); `Adhoc::index($filter)` supports `proposals` (status=ProposalSent) and `events` (event_type=Event); "Adhoc Revenue" cross-links to the existing `reports/adhoc_sales` report.
- [x] **Activities** - brand-new standalone top-level module (previously only nested inside `Accounts::add_activity()`, which still works unchanged). New `Activities` controller: `index('mine'|'team')` and `add()` for a generic account-optional activity log. Migration `009` makes `dso_activities.account_id` nullable to support this.
- [x] Targets - "Set Targets" confirmed as a menu-label addition only; `Targets::add()` was already correctly gated, no code change needed.
- [x] Collections - `credit_limits()` (contract credit limit vs outstanding balance per account), `invoices()` (invoice-framed list), `statements()` (staff-side per-account statement, mirroring the Corporate Portal's own statement view for internal use).
- [x] Property Management - `Properties::availability($id)` (Sales-Coordinator-gated `is_bookable` flag + blackout-dates list, migration `010`; deliberately scoped as contract-eligibility-adjacent settings, NOT a full date-by-date room-inventory calendar, which is out of scope for this BRD item per `implementation.md`'s existing "availability = contract eligibility" architecture note).
- [~] AI Sales Assistant - `predictions()`/`next_best_actions()` filter `dso_ai_recommendations` by two new enum values (migration `008` extends the `type` column) with a friendly "no data yet" empty state, since no generator produces `Prediction`/`NextBestAction` rows yet - that generator is a separate, larger future item, not built in this pass, and no fake data was fabricated to fill the gap. `analytics()` is a real aggregate (counts by type/status).
- [x] **Administration (net-new)** - full dynamic RBAC: `dso_roles`/`dso_permissions`/`dso_role_permissions` tables (migration `008`), `Dso_Controller::require_permission()` alongside the legacy `require_role()` (kept working forever, all ~40 existing call sites untouched), `Admin/Roles.php` + `Admin/Users.php` for Users & Roles. **Teams** with territory: `dso_teams`/`dso_team_properties`/`dso_team_accounts`, `Admin/Teams.php`, and optional territory-scoped filtering wired into `Leads`/`Accounts`/`Contracts`/`Reservations` list queries (a team with zero assigned territory rows means "no restriction" - existing seeded data behaves exactly as before). **Integrations** - `Admin/Integrations.php` reads/rewrites `application/config/dso_integrations.php` directly, no new table. **Notification Center** - `Admin/NotificationCenter.php`, admin list + broadcast form over the existing `dso_notifications` table.

New migrations this session: `dyafa_sales_os_migration_008_admin_rbac_teams.sql`, `dyafa_sales_os_migration_009_activities_generic.sql`, `dyafa_sales_os_migration_010_property_availability.sql` - all idempotent/information_schema-guarded, run in order after `007`.

## Remaining Backlog (as of the prior session)
- A real AI/ML generator producing `Prediction`/`NextBestAction` recommendation rows (views exist, generator does not). **Closed this session — see below.**
- A full date-by-date room-inventory/availability calendar (current Availability Settings is a simple bookable-flag + blackout-dates list, not per-room per-night inventory). Still out of scope - unchanged.
- Migrating the ~40 legacy `require_role()` call sites to `require_permission()` - both work side by side indefinitely; only new Administration-era code uses the new gate. Unchanged.

## Audit Trail, Security Hardening & Presentation Pass (this session, 2026-07-23)

Closes every P0/P1 item in `enhance.md` and section A/B/C of `todolist.md`
(both cross-checked against live code, not re-guessed - one correction:
`enhance.md`'s "Leads tab 404" claim was already stale, fixed by an earlier
session). Full detail in `README.md` and `implementation.md`; summary:

- [x] **Dead routes removed** - `getProperty`/`getAllProperties` in `routes.php` deleted after confirming no `Api` controller or external caller exists anywhere in the codebase.
- [x] **Soft delete + shared audit trail** - `deleted_at` column (migration `012`) on Contracts, Accounts, Adhoc Sales, Properties, Collections, Targets, Roles, Teams; new `dso_audit_log` table written from `Dso_Controller::audit()`/`soft_delete_row()` at every `add()`/`edit()`/`delete()` on those 8 entities; viewable at new **Administration > Audit Log** (`Admin/AuditLog.php`).
- [x] **`Portal::user_edit($id)`** added, mirroring `user_add()`'s validation; optional password field.
- [x] **`push_to_reporting()` extended** to all 14 reports (was 7); each new report view gained the "Push to Reporting Platform" button.
- [x] **AI Predictions / Next Best Actions generator built** - `Dso_sales_assistant::find_churn_risk_accounts()` (booking-cadence-based) and `find_accounts_needing_next_action()` (activity-recency-based), wired into `generate_all_recommendations()`.
- [x] **Integration API keys encrypted** - new `dso_integration_credentials` table/model (same boundary as `Dso_ai_providers_model`); `Admin/Integrations.php` and the 4 live-mode HTTP call sites (PMS/Finance/Payment/Reporting) updated accordingly.
- [x] **Mandatory 2FA for CorporateFinance** - new dependency-free RFC 6238 `Dso_totp.php` library, `dso_users.totp_secret_encrypted`/`totp_enabled` columns, forced enrollment (`Portal::setup_2fa()`) + verification (`Portal::verify_2fa()`) flow.
- [x] **Pagination** - `Dso_Controller::paginate()` (CI native Pagination library) added to Leads/Reservations/Notifications `index()`, with a matching `count_all()` on each model; these 3 tables excluded from the existing global client-side DataTables auto-init in `layout/footer.php` to avoid double pagination controls.
- [x] **Presentation**: favicon (inline SVG data URI); dedicated minimal guest layout (`dyafa/layout/guest_header.php`/`guest_footer.php`) for login/2FA screens instead of the full authenticated-app shell; Chart.js (CDN) added to Daily/HOD dashboards (MTD revenue trend, target-achievement gauge, top-5-accounts bar chart), backed by new `Dso_reservations_model::daily_revenue_trend()`; ~15 remaining raw-hex inline styles replaced with the existing CSS variables (`.dso-badge.danger`/`.dso-badge.warning` added); legacy CMS sidebar link visually de-emphasized (dimmed, divider, external-link icon); `portal/invoice_pdf.php` given a proper branded header/footer/status-badge stylesheet.

New migration this session: `dyafa_sales_os_migration_012_audit_soft_delete_security.sql` (idempotent, run after `011`).

## Remaining Backlog (explicitly out of scope this session)
- A full date-by-date room-inventory/availability calendar (current Availability Settings is a simple bookable-flag + blackout-dates list, not per-room per-night inventory).
- Migrating the ~40 legacy `require_role()` call sites to `require_permission()` - both work side by side indefinitely; only new Administration-era code uses the new gate.
- A real internal REST/JSON API, automated test coverage, and rate limiting/backoff on external calls - each a genuinely large, separate initiative rather than a fit alongside this pass (see `todolist.md` section E for detail and future priority targets).
